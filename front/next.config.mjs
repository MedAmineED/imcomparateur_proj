/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.pixabay.com', 'localhost', 'localhost:8000', 'www.visana.ch'], // Allow images from pixabay
      },
};

export default nextConfig;
